# TODO - Fashion Hut Admin Gallery Upload

- [x] Inspect existing repo structure and current gallery/chatbot behavior.
- [x] Add `admin-panel.js` to `index.html` so admin UI can load on the site.
- [x] Update `fashionhut-complete.js` to refresh public gallery when admin uploads/removes images (via `fh-admin-gallery-changed` event).
- [ ] Add an Admin button / entry point on the public page and render admin UI (login + upload + remove).
- [ ] Ensure admin UI overlays/embeds cleanly without affecting layout.
- [ ] Manual test: login with phone+password, upload images, verify immediate appearance for users; remove images and verify disappearance.

